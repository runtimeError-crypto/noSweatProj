const port = 3000
mongoose.connect("mongodb://localhose:27017/REST");

const app = express();
app.get("/post", (req, res){
	p[ost.find({}).then(post => {
		post = post
	})]
}
let app = require mongoose


app.request = (req, res) {
	{
		app.post = postP{})
		)
}
}
